# install node.js

>$ git clone https://github.com/nodejs/node.git

>$ sudo chmod -R 755 node

>$ cd node

>$ sudo ./configure

>$ sudo make

>$ sudo make install

>$ node -v

# install npm tools

>$ sudo apt-get install npm

>$ npm -v
